services:
  vibe-nav:
    build: .
    container_name: vibe-nav
    ports:
      - "${PORT:-9000}:3456"
    env_file:
      - .env.docker
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
