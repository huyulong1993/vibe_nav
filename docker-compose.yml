services:
  vibe-nav:
    build: .
    container_name: vibe-nav
    ports:
      - "${PORT:-3456}:9000"
    env_file:
      - .env.docker
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/vibe-nav/data
    restart: unless-stopped
