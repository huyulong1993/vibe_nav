# === Vibe Nav Docker 部署环境变量 ===
# 复制此文件为 .env.docker 并修改配置

# 管理后台登录密码
ADMIN_PASSWORD=你的密码

# 会话签名密钥（生成方式: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"）
AUTH_SECRET=你的随机密钥

# 服务端口（可选，默认 3456）
PORT=3456
